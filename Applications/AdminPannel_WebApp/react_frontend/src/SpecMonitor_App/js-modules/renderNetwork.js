/*
    Use this to render Network sections of the final HTML
        
    -Network interfaces currently up
    -sent packages
    -received packages
    -static IP
    -localhost name
*/


function render () {
    return (
        <div>
            empty
        </div>
    );
}

export default render