/*
    Use this to render Infos sections of the final HTML
*/

function render () {
    return (
        <div>
            <p>Infos</p>

            empty
        </div>
    );
}

export default render